o	Accept only valid hexadecimal characters
o	Accept a maximum of 2 hex digits per operand

o	X Perform addition on two hex numbers
o	Perform subtraction on two hex numbers
o	Perform multiplication on two hex numbers
o	Perform division on two hex numbers

o	Return results in hexadecimal format
o	Return a maximum of 4 hex digits
o	Not return negative values 
o	Not return decimal values

o	Handle division by zero gracefully
o	Display a meaningful message when an invalid operation is attempted"
